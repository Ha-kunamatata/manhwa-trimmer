# 만화 재단기 — 작업 노트

Claude Code로 이 저장소를 이어서 작업할 때 필요한 맥락.

## 이 프로젝트가 하는 일

만화를 자르고(**편집**), 만화책처럼 읽는다(**뷰어**). 처음 화면에서 두 갈래로 나뉜다.
편집은 길게 캡처한 이미지를 잘라 파일로 남기고, 뷰어는 폴더나 GitHub 비공개 저장소에서 읽는다.
**뷰어가 읽는 것이 잘려 있을 필요는 없다** — 긴 캡처면 편집과 같은 판단으로 알아서 잘라 읽는다.
전부 브라우저에서 처리하며 서버가 없다.

## 설계 원칙

**핵심 로직에 DOM을 섞지 않는다.** `src/core/*`는 순수 함수만 두고 `node --test`로 검증한다.
DOM이 필요한 것은 전부 `src/ui/*`에 있다. 새 알고리즘은 `core`에 넣고 테스트를 먼저 붙일 것.

**뷰어는 페이지의 출처를 모른다.** 스트립 조각이든 폴더 파일이든 저장소 blob이든
`sources.js`의 공급원 하나로 감싸서 건넨다. 새 출처를 붙일 때 `reader.js`를 고칠 일이 있다면
추상화가 잘못된 것이다.

**자르는 판단은 `core/layout.js` 한 곳에만 있다.** 편집과 뷰어가 같이 쓴다. 뷰어에서 자른
결과가 편집에서 자른 결과와 달라지면 그건 버그가 아니라 배신이므로, 복사해 쓰지 말 것.
`detectBox`와 `planPages`가 나뉘어 있는 이유는 비용이 달라서다 — 재는 건 느리고 한 번,
배치는 싸고 컨트롤을 움직일 때마다 일어난다.

**넘김의 형태는 `core/curl.js`에 순수 함수로 있다.** 종이가 늘어나지 않는다는 것이 전부다 —
호 길이 보존이 무너지면 여닫이문처럼 보인다. 상수를 만지기 전에 `curl.test.mjs`를 볼 것.

**뷰어 상태 변화는 전부 큐를 지난다.** 페이지 넘김, 판형 토글, 화 전환, 리사이즈가 모두
`enqueue`로 직렬화된다. 하나라도 빠지면 렌더 토큰이 어긋나 화면과 페이지 번호가 서로
다른 곳을 가리킨다 (아래 함정 참고).

**추측하지 말고 측정한다.** 이 도구는 "만화 페이지는 대충 이 정도 높이일 것"이라는 가정으로
여러 번 실패했다. 지금 방식은 판형에서 나온 후보를 실제 이미지에 대보고 채점해 고른다.
비슷한 판단이 필요하면 같은 방식을 따를 것 — 상수를 새로 박아넣지 말고 후보를 채점하라.

## 과거에 실제로 물렸던 함정

이미 겪고 고친 것들이라 다시 밟지 않도록 기록해둔다.

- **이미지 폭 ≠ 페이지 폭.** 캡처에는 좌우 여백이 있고, 두 페이지 펼침이면 폭이 두 배다.
  판형 비율은 반드시 *측정된 페이지 폭*에 곱해야 한다.
- **주기 검출의 옥타브 오류.** 진짜 간격의 2배(또는 3배)로 잘라도 경계는 똑같이 여백에 떨어진다.
  점수가 비슷하면 *더 잘게 나뉘는 쪽*을 골라야 한다. (`autoFit`의 tied 필터)
- **여백은 두께로 구분한다.** 흰 줄인지만 보면 컷 사이 얇은 틈과 페이지 사이 두꺼운 여백을
  구분할 수 없다. `buildBandMap`이 각 행이 속한 여백 띠의 두께를 기록한다.
- **균등 분할은 구조적으로 어긋난다.** 스트립이 `페이지-여백-페이지…`라 전체를 n등분하면
  ±여백/2 만큼 틀리고 양 끝에서 최대가 된다. 그래서 스냅이 필요하다.
- **스냅은 체인으로.** 각 선을 독립적으로 스냅하면 페이지 높이가 다를 때 탐색 범위를 벗어난다.
  다음 선은 앞 선이 실제로 놓인 위치에서 재계산한다.
- **CSS 미디어 쿼리는 특이도를 올리지 않는다.** 오버라이드는 원본 규칙 *뒤에* 와야 한다.
  (`.rail`의 sticky 해제가 이것 때문에 한 번 무시됐다)
- **저장은 연속 요청이 막힌다.** 낱장 저장은 rate limit에 걸리므로 백오프 후 재시도한다.
  기본 경로는 PDF 한 파일이다.
- **저장이 Artifact 안에서만 되던 시절이 있었다.** `window.claude.use("downloads")`에만
  의존해서, 정작 사람들이 실제로 쓰는 배포된 PWA에서는 내보내기가 통째로 죽어 있었다.
  지금은 호스트가 없으면 브라우저 다운로드로 떨어진다. 호스트 API에 기대는 기능을 새로
  붙인다면 없을 때의 길도 같이 낼 것. 15.4MB 상한도 호스트의 제약이지 브라우저의 것이
  아니므로 `dl.limit`로 구분한다 — 웹에서는 긴 만화도 화질을 깎지 않는다.
- **라벨은 픽셀과 함께 움직여야 한다.** 화 이름을 `render()` 밖에서 갱신했더니, 렌더가
  무효화됐을 때 아직 보이지도 않는 화의 이름이 화면에 남았다. `setTitle`과 `showCount`는
  렌더가 실제로 그린 뒤 같은 자리에서 부른다.
- **큐를 안 지나는 렌더 하나가 전부를 망친다.** 리사이즈 렌더만 큐 밖에 있었더니 토큰을
  올려 화 전환 렌더를 무효화했고, 페이지 번호가 이전 화의 마지막 쪽에 멈췄다.
- **실패를 삼키지 말 것.** 큐를 `chain.then(fn, fn)`으로 썼다가 앞 단계가 실패하면 같은
  단계가 거부 핸들러로 **한 번 더** 실행됐다. 페이지가 두 칸씩 넘어갔고 오류는 보이지 않았다.
- **거부된 프로미스를 캐시에 남기지 말 것.** 디코딩 실패가 캐시에 그대로 남으면 그 페이지는
  영영 안 열린다. 한 번의 딸꾹질이 영구 장애가 된다.
- **서비스 워커가 남의 캐시를 지운다.** `activate`에서 자기 것 외를 전부 지우면 저장소에서
  받아둔 페이지 캐시까지 날아간다. `KEEP` 목록으로 지켜야 한다.
- **뷰어에 넣을 폴더가 잘려 있으리라 가정하지 말 것.** 처음엔 뷰어를 "이미 잘라둔 만화"
  전용으로 만들었는데, 정작 사람이 가진 건 자르지 않은 긴 캡처였다. 지금은 첫 이미지를
  한 번 열어보고 `looksLikeStrip`으로 갈라, 캡처면 `slicer.js`가 같은 판단으로 잘라 읽는다.
- **캡처 한 장은 화 하나다.** 그래서 캡처 모드에서는 계층이 한 단 올라간다 — 캡처를 담은
  폴더가 시리즈가 되고 파일이 화가 된다(`regroupAsStrips`). 낱장 규칙을 그대로 쓰면
  시리즈 폴더가 화로 잡혀 목록이 통째로 어긋난다. 실제로 이걸 놓쳐 테스트가 잡아냈다.
- **캐시 단위가 소스마다 다르다.** 낱장은 파일=페이지라 페이지 기준으로 창을 잡지만,
  긴 캡처는 파일 하나가 페이지 수십 장이고 디코딩하면 수십 MB다. `slicedSource`는 파일을
  두 개까지만 쥔다. 페이지 기준으로 잡으면 같은 거대한 캔버스를 수십 벌 붙들게 된다.
- **잰 결과는 남기고 픽셀은 버린다.** 캡처를 잘라 얻은 건 사각형 목록뿐이고, 그릴 때 파일을
  다시 연다. 디코딩한 스트립을 들고 있으면 화 몇 개만 열어도 메모리가 바닥난다.
- **슬라이스마다 반투명을 칠하면 줄무늬가 된다.** 틈을 막으려 1px씩 겹쳐 그리는데, 그 겹친
  자리에 알파가 두 번 얹혀 어두워진다. 이음매가 전부 세로줄로 보였다. 빛은 슬라이스가 다
  그려진 뒤 그라디언트 하나로, 종이 실루엣에 클립해서 올린다.
- **들린 종이를 크게 그리면 잘린다.** 캔버스가 책과 같은 크기라 배율이 1을 넘는 순간 위아래가
  잘려나간다. `foreshorten`은 1을 넘지 않는다(테스트로 고정).
- **장수가 바뀌는 전환은 모션을 건너뛴다.** 표지 한 장 → 펼침 두 장에서는 시트 크기도 책등
  위치도 정해지지 않는다. 억지로 그리면 화면 밖으로 나가거나 뭉갠다. `sameShape` 참고.
- **마우스 움직임으로 UI를 되살리면 탭과 싸운다.** 클릭은 같은 자리에서 mousemove를 함께
  보내므로, 모든 움직임에 반응했더니 탭 직전에 UI가 켜지고 탭이 그것을 다시 껐다. 가운데
  탭으로 UI를 켤 수가 없었다. 실제로 움직였을 때만(8px) 반응한다.
- **비공개 저장소는 트리 한 방이면 된다.** 폴더별로 Contents API를 타고 내려가지 말 것.
  `git/trees?recursive=1` 한 번이면 전체 목록이 나온다. 페이지는 SHA로 받아 캐시한다 —
  SHA는 내용 그 자체라 만료를 신경 쓸 필요가 없다.

## 테스트

```bash
npm test           # core 단위 테스트 — 빠르다, 항상 먼저 돌릴 것
npm run test:e2e   # Playwright, 실제 브라우저
```

`tests/fixtures.mjs`가 합성 스트립을 만든다(이미지 라이브러리 불필요).
`makeStats`는 `analyseStrip`이 낼 통계를 그대로 만들어주고,
`makePng`는 브라우저 테스트용 실제 PNG를 zlib으로 인코딩한다.

**픽스처 자체의 버그를 조심할 것.** 과거에 페이지 높이만 줄이고 패널 크기는 그대로 둬서
패널이 여백을 침범했고, 멀쩡한 알고리즘이 실패로 나온 적이 있다.
테스트가 실패하면 알고리즘을 고치기 전에 픽스처가 유효한지부터 확인하라.

**e2e 픽스처는 워커마다 따로 쓴다.** 스펙 파일은 워커 8개에서 각각 평가되므로 경로를
공유하면 여덟 프로세스가 브라우저가 읽는 중인 PNG를 동시에 덮어쓴다. 반쯤 쓰인 파일을
받은 디코더가 실패하고, 그게 "가끔 페이지가 안 열리는 앱 버그"처럼 보인다.
실제로 이걸 앱 문제로 착각해 한참 헤맸다 — 간헐적 e2e 실패는 픽스처부터 의심할 것.

## 빌드 두 가지

- **PWA** — `index.html`이 ES 모듈을 그대로 불러온다. GitHub Pages로 배포된다.
- **단일 파일** — `npm run build`가 `dist/artifact.html`을 만든다.
  Claude Artifact는 외부 요청이 막혀 있어 전부 인라인되어야 한다.
  `scripts/build-artifact.mjs`가 모듈 그래프를 펼쳐 하나로 합친다.
  **UI를 고쳤으면 두 경로 모두 확인할 것.**

빌드가 `window.__manhwaArtifact`를 심고 셸이 그걸 보고 GitHub 진입점을 숨긴다.
외부 요청이 필요한 기능을 새로 붙인다면 같은 처리가 필요하다.
새 모듈을 만들면 `sw.js`의 `ASSETS`에도 넣고 `CACHE` 버전을 올릴 것.

## 아직 안 한 것

- 자르지 않고 그대로 내리읽는 진짜 웹툰 모드 (지금의 스크롤 모드는 *자른 페이지*를 세로로
  쌓는다. 컷 없이 흐르는 세로 만화는 여전히 페이지 판형을 전제로 잘린다)
- 좌우 여백을 페이지마다 따로 잡기 (지금은 스트립 전체에 하나)
- 편집에서 여러 이미지를 한 번에 처리 (지금은 하나씩)
- 뷰어에서 자동으로 자른 경계를 손으로 고치기 (지금은 자동 판단만, 고치려면 편집으로)
- 핀과 읽던 위치를 기기 간 동기화 (지금은 localStorage, 기기별)
- 잰 결과를 앱을 닫아도 유지 (지금은 세션 메모리, 다시 열면 다시 잰다)
- 저장소에서 화 전체를 미리 받아두는 오프라인 담기 (지금은 읽은 페이지만 캐시)
